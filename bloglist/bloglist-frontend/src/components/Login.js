import LoginForm from './LoginForm'

const Login = ({
  username,
  password,
  setUsername,
  setPassword,
  handleLogin,
}) => {
  return (
    <div>
      <LoginForm />
    </div>
  )
}

export default Login
